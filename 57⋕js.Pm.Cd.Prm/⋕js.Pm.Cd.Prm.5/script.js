/*⋕js.Pm.Cd.Prm.5*/

let num = 123501;
let numStr = String(num);
let sumFirst = Number(numStr[0]) + Number(numStr[1]) + Number(numStr[2]);
let sumSecond = Number(numStr[3]) + Number(numStr[4]) + Number(numStr[5]);

if(sumFirst == sumSecond){alert('Yes')}
else (alert ('No'));



